name: Deploy to Modal

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Verify project files
        run: |
          if [ ! -f "main.py" ]; then
            echo "❌ Error: main.py not found"
            exit 1
          fi
          if [ ! -f "requirements.txt" ]; then
            echo "❌ Error: requirements.txt not found"
            exit 1
          fi
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install Modal
        run: pip install -r requirements.txt
      
      - name: Verify Modal authentication
        env:
          MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID }}
          MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET }}
        run: |
          if [ -z "$MODAL_TOKEN_ID" ] || [ -z "$MODAL_TOKEN_SECRET" ]; then
            echo "❌ Error: MODAL_TOKEN_ID or MODAL_TOKEN_SECRET is not set"
            exit 1
          fi
      
      - name: Deploy to Modal
        env:
          MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID }}
          MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET }}
        run: |
          # Clean tokens from any whitespace (prevents "Token ID is malformed" errors)
          export MODAL_TOKEN_ID=$(echo "$MODAL_TOKEN_ID" | tr -d '[:space:]')
          export MODAL_TOKEN_SECRET=$(echo "$MODAL_TOKEN_SECRET" | tr -d '[:space:]')
          
          modal deploy main.py
